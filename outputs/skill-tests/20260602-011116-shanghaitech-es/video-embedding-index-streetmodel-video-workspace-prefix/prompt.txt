请测试 `video-embedding-index` skill 的 Agent 真链路，测试用例：`video-embedding-index-streetmodel-video-workspace-prefix`。

测试说明：Force the video path to a writable local prefix so the skill reaches StreetModel /embed with a GPU-side path that is expected to be inaccessible.

必须严格执行：
1. 先调用 read_file 读取 `/mnt/skills/custom/video-embedding-index/SKILL.md`。
2. 再调用 bash 执行下面这段命令，不要改路径，不要省略任何输出文件。
3. 执行完成后读取 `/mnt/user-data/outputs/video-embedding-index-streetmodel-video-workspace-prefix/result.json`，并在最终回复中返回这个 JSON 的核心字段和简短结论；如果失败，保留 error_code/message/detail。

```bash
mkdir -p /mnt/user-data/outputs/video-embedding-index-streetmodel-video-workspace-prefix /mnt/user-data/workspace/streetmodel_shared
python /mnt/skills/custom/video-embedding-index/scripts/run.py --embedding-provider streetmodel --video-id shanghaitech-07-007-agent-test-streetmodel-workspace --video-uri /mnt/datasets/shanghaitech-agent-eval/07_007.avi --copy-video-to-shared --deerflow-path-prefix /mnt/user-data/workspace/streetmodel_shared --streetmodel-path-prefix /nfsdat2/home/xhuangslm/shared_videos/codex_unmounted_workspace --target-index huangxiao-shanghaitech-vector-agent-test --owner huangxiao --timeout-seconds 120 --config /mnt/skills/custom/configs/deerflow_config.json --output /mnt/user-data/outputs/video-embedding-index-streetmodel-video-workspace-prefix/result.json
```

成功判定：必须能看到 read_file 读取该 SKILL.md、bash 执行该 skill 入口、并生成 `/mnt/user-data/outputs/video-embedding-index-streetmodel-video-workspace-prefix/result.json`。
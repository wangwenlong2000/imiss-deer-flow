"""Vendored delivery-package code — a verbatim lift, do not edit.

``violation_detection/model_detectors/`` from
``violation_detection_model_normalized_723.zip``, copied unchanged by
``make compliance-assets`` so a package upgrade is a straight overwrite.

Fixes and improvements belong upstream in the delivery package, not here: local
edits would be silently reverted by the next `make compliance-assets` run.
"""

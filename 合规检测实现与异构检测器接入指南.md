# 合规检测实现总览与异构检测器接入指南

本文面向需要阅读、维护或扩展 DeerFlow 合规检测链路的开发者，覆盖：

- 当前合规检测系统如何从请求进入到处置落地；
- 检测器、场景解析、策略矩阵、闸门和审计之间的职责边界；
- 纯 Python、重依赖、非 Python、独立 GPU 服务等异构检测器的接入方式；
- 新检测器从设计、实现、注册到验收的完整清单。

专项细节可继续参考：

- [检测器接入指南](../compliance-detector-integration-guide.md)
- [新检测器入门指南](new_detector_onboarding_guide.md)
- [检测器契约 JSON Schema](../compliance-detector-contract.schema.json)
- [OneCityAgent–DeerFlow 合规契约](onecity_deerflow_compliance_contract.md)

## 1. 先记住四条原则

1. **检测器只负责识别，不负责处置。** 检测器返回“命中了什么、在哪里、置信度、依据和证据”；refuse、desensitize、aggregate 等动作由策略矩阵统一决定。
2. **所有实现技术都收敛到同一个契约。** 正则、词典、轻量模型、重型模型、外部服务和其他语言实现，对引擎都表现为 DetectionHit。
3. **检测器只能看冻结的检测单元。** 它不能读取 Agent 状态、消息历史、权限对象、评测标签或其他检测器的内部状态。
4. **场景不可信或检测失败不能被伪装成放行。** 场景缺失进入 _unknown；检测异常写入诊断；闸门级故障按 fail_mode 处理。

## 2. 整体架构

~~~text
请求/工具结果/模型回答
          │
          ▼
      Normalizer
  统一成 DetectionUnit
          │
          ▼
 ComplianceEngine
  ┌───────┼─────────────────────────┐
  │       │                         │
  │   SceneResolver             IntentGuard
  │   解析使用场景                InputGate 意图合并
  │       │                         │
  └───────┴──────────────┬──────────┘
                         ▼
                    DetectorRouter
             按 gate × data_type × cost 筛选
                         │
                         ▼
                  DetectorRegistry
                         │
          ┌──────────────┼──────────────┐
          ▼              ▼              ▼
      inprocess     subprocess_cli   http_service
      Python对象     子进程/任意语言     独立服务/GPU
          └──────────────┼──────────────┘
                         ▼
                  DetectionHit 校验
                         │
                         ▼
                   PolicyMatrix
           violation × gate × scene → actions
                         │
          ┌──────────────┼──────────────┐
          ▼              ▼              ▼
      闸门处置       审计 JSONL       SSE/前端元数据
   脱敏/改写/拒答       依据与诊断       retract/notice
~~~

核心代码位置：

| 层 | 主要文件 | 职责 |
|---|---|---|
| 边界契约 | backend/packages/harness/deerflow/compliance/contract.py | 冻结的数据类、检测器协议、枚举、命中校验、JSON Schema 导出 |
| 输入归一化 | compliance/normalizers/ | 将用户输入、上传文件、SkillResult、模型回答转成 DetectionUnit |
| 注册发现 | compliance/registry.py | 扫描 manifest、校验能力、构建适配器 |
| 适配器 | compliance/adapters/ | 把三种实现方式统一成 detect(unit, ctx) |
| 路由 | compliance/router.py | 按闸门和数据类型筛选检测器，按开销排序 |
| 编排 | compliance/engine.py | 预算、隔离执行、异常诊断、命中过滤、策略决策、审计 |
| 场景与意图 | compliance/scene.py、intent.py | 解析授权绑定的场景；InputGate 合并高风险意图 |
| 策略 | compliance/policy.py、config/compliance/policy_matrix.yaml | 将违规类型映射为动作和依据 |
| 动作执行 | compliance/actions.py | 将脱敏、聚合、改写、拒答落到文本 |
| 闸门 | agents/middlewares/compliance_*_gate_middleware.py | 将引擎接入 Agent 的输入、工具上下文和模型输出 |
| 审计 | compliance/audit.py | 记录命中、动作、场景、依据、诊断和审计编号 |

## 3. 一次检测如何运行

### 3.1 归一化

三类闸门看到的数据形状不同，因此检测器不直接处理 LangChain 消息或业务对象：

| 闸门 | 输入 | 默认归一化方式 |
|---|---|---|
| InputGate | 用户问题、文本上传 | UserInputNormalizer；可读文本文件按文件生成单元 |
| ContextGate | 工具返回的 SkillResult | SkillResultNormalizer；每条 evidence 生成一个单元，文本和结构化字段分开 |
| OutputGate | 最终 AIMessage | LlmOutputNormalizer；面向用户的回答作为文本单元 |

DetectionUnit 是检测器唯一需要处理的业务输入：

~~~python
DetectionUnit(
    unit_id="req-1:e-001",
    gate="ContextGate",
    data_type="telecom",
    text_items=(TextItem(
        item_id="t-0001",
        text="业务文本或证据描述",
        source="e-001",
    ),),
    field_items=(FieldItem(
        item_id="f-0001",
        path="metadata.station",
        value="station-value",
        source="e-001",
    ),),
    raw={"kind": "skill_evidence"},
)
~~~

约束：

- text_items 用于自由文本检测；source 用于定位来源。
- field_items 使用点号路径，例如 metadata.camera_id；不要把整个嵌套对象塞进一个不可定位的字符串。
- raw 只用于有限的来源信息；不能依赖它访问 Agent 状态或任意宿主资源。
- DetectionUnit 由引擎交给检测器前会冻结/深拷贝，检测器不得修改输入。
- 归一化会截断超大文本和字段；引擎还会限制最大单元数和总耗时，截断必须进入诊断。

### 3.2 场景和意图

检测结果和业务处置不是一回事：

- SceneResolver 根据可信的用户、组织、资源归属、操作、目标受众和权限绑定解析 self_use、internal_org、cross_org、public_release、research_anon。
- scene_hint 只是提示，不能单独选择策略列。
- 当前默认 NullSceneResolver 会将无法确认的请求放入 _unknown；生产环境只有在可信上游/IAM 绑定完成后才应启用 trusted_upstream。
- InputGate 还会用 IntentGuard 合并“敏感实体 + 高风险意图”。例如，用户请求“将手机号脱敏后分析”不应仅因出现手机号就当作高风险公开行为。
- ContextGate 和 OutputGate 处理已经检索或生成的内容，命中不会因为低风险意图而自动消失。

检测器不应根据自然语言自行猜测场景或权限，也不应把意图判断复制一份到自己的代码中。

### 3.3 路由和检测

DetectorRouter 只把满足以下条件的已启用检测器交给当前单元：

1. manifest 声明了当前 gate；
2. data_type 被检测器接受；
3. 检测器已成功注册并启用。

data_types: null 表示接受任意数据类型；如果检测器声明了具体类型，而单元的 data_type 为空，路由不会猜测并交给它。

检测器按 cost_hint 以 light → medium → heavy 排序。每个检测器单独计时、单独捕获异常：

- 一个检测器崩溃，不会阻止其他检测器继续运行；
- 超时、协议错误、HTTP 错误会进入 diagnostics.detector_errors 和 warning；
- 总预算耗尽时停止后续检测，并明确记录未检查的单元/检测器组合；
- 检测器返回了 manifest 未声明的违规类型或闸门命中，会被丢弃并记录 warning。

多个检测器可以声明同一个违规类型，例如规则检测器与模型检测器并行运行。命中是累加的，不应互相覆盖。

### 3.4 策略和动作

PolicyMatrix 读取 config/compliance/policy_matrix.yaml，对每个不同的违规类型计算：

~~~text
violation_type × gate × resolved_scene → actions
~~~

矩阵还负责提供合规依据。多个违规类型的动作会合并，并按动作强度排序；弱动作不能覆盖强动作。

检测器不能返回动作。以下职责属于策略/动作层：

- warn、report、role_check、manual_review：提示、审计或人工队列，不直接修改文本；
- desensitize：优先按照 RiskLocation 做局部掩码；
- aggregate：将明细转换为聚合提示/结果；
- rewrite：当前实现对风险片段做掩码并标注“合规改写”，不在输出闸门内同步调用大模型；
- refuse：用统一的拒答文案替换整段内容；
- 脱敏或改写找不到可用风险位置时，默认保守回退为拒答。

因此，RiskLocation 不是可选的装饰字段，而是动作执行和审计定位的关键输入。

### 3.5 输出闸门和流式场景

OutputGate 有两层安全边界：

1. 流式回答已显示部分内容时，发送 compliance_retract 事件，前端用安全替换内容定位并替换消息；
2. 在持久化、标题、原始 transcript 和 checkpoint 之前，把最终消息替换为处置后的版本。

retract 事件不能重新携带被撤回的原文。消息 ID 必须保持不变，否则状态 reducer 可能追加新消息而保留原始风险内容。

## 4. 检测器边界契约

检测器作者只应依赖 contract.py 中的类型：

~~~python
from deerflow.compliance.contract import (
    DetectContext,
    DetectionHit,
    DetectionUnit,
    RiskLocation,
)

class MyDetector:
    detector_id = "my_detector"

    def setup(self, params):
        ...

    def detect(self, unit: DetectionUnit, ctx: DetectContext) -> list[DetectionHit]:
        ...
~~~

Detector 是结构化协议，不要求继承基类。setup 通常在首次使用前完成一次，具体时机由适配器决定；适合编译正则、加载轻量模型或建立客户端，但不要在 import 时加载大模型或访问网络。

### 4.1 输入：DetectContext

检测器可获得：

- 当前 gate；
- 可选的 data_type（在 DetectionUnit 中）；
- 当前请求的有限用户上下文；
- InputGate 意图摘要；
- 本次检测的 budget_ms。

不要把 ctx 当作策略接口。尤其不要使用未经授权的 scene_hint 推导放行或拒答。

### 4.2 输出：DetectionHit

每个命中至少要提供：

~~~python
DetectionHit(
    detector_id="my_detector",
    violation_type="domain",
    confidence=0.93,
    severity="high",
    risk_locations=(
        RiskLocation(
            kind="char_span",
            locator="12:28",
            text="需要遮蔽的原文片段",
            entity_type="example_entity",
        ),
    ),
    reason_code="my_rule_positive",
    evidence={"rule": "bounded-rule-id"},
    basis=("项目规则",),
)
~~~

字段要求和建议：

| 字段 | 要求 |
|---|---|
| detector_id | 必须非空，并与 manifest 一致 |
| violation_type | 只能使用契约中的十类违规类型 |
| confidence | [0,1]；模型应说明校准方式，规则检测器也要保持稳定口径 |
| severity | info、low、medium、high、critical 之一 |
| risk_locations | 尽量给出至少一个可复现位置；位置越准确，脱敏越局部 |
| reason_code | 稳定、可聚合、不可包含原文的规则码 |
| evidence | 仅放有限元数据、分数、版本或规则 ID，不放整篇原文、凭证或大对象 |
| basis | 检测器明确知道的依据；最终策略依据仍由 Policy Matrix 补齐 |

位置类型：

| kind | locator 例子 | 适用对象 |
|---|---|---|
| char_span | 12:28 | 自由文本字符区间 |
| field_path | metadata.camera_id | 结构化字段 |
| frame | frame:120 或稳定帧标识 | 视频 |
| bbox | 稳定的框坐标字符串 | 图像/视频中的区域 |
| coordinate | 可复现的坐标定位描述 | 地理数据 |

优先同时填写 text。动作层会优先按风险文本替换，无法使用文本时再使用字符区间。不要为了“有证据”复制整个输入文档。

## 5. Manifest、注册和配置

每个检测器目录只需要一个可发现的 manifest.yaml：

~~~yaml
contract_version: "1.0"
detector_id: my_detector
adapter: inprocess
entry: deerflow.compliance.detectors.my_detector.detector:MyDetector

violation_types:
  - domain

gates:
  domain:
    - ContextGate
    - OutputGate

data_types: null
cost_hint: light

description: >-
  有边界的能力说明，不写处置动作。

default_params:
  threshold: 0.80
~~~

Registry 启动时会校验：

- contract_version 主版本兼容；
- detector_id 非空且全局唯一；
- violation_types 属于契约十类；
- 每个违规类型都有 gate 声明，且 gate 合法；
- data_types 是列表或 null；
- cost_hint 是 light、medium、heavy；
- heavy 检测器不能声明 ContextGate；
- 适配器所需的 entry、command 或 endpoint 存在。

运行时默认以非严格模式构建 Registry：单个坏 manifest 会记录错误并跳过，其他检测器仍可启动；CI 和专项验收必须使用 strict=True，让配置错误直接失败。

检测器的能力和运行方式写在 manifest；config/compliance/detectors.yaml 只负责开关和参数覆盖：

~~~yaml
detectors:
  - id: my_detector
    enabled: false
    params:
      threshold: 0.85
~~~

未出现在中心配置中的已发现检测器默认启用；为了灰度上线，建议显式写 enabled: false，完成测试后再打开。

### 5.1 四个容易混淆的配置位置

| 内容 | 应放在哪里 |
|---|---|
| 检测器 ID、违规类型、闸门、数据类型、开销、适配器 | 本检测器的 manifest.yaml |
| 默认参数 | 本检测器的 manifest.yaml |
| 环境差异、灰度开关、阈值覆盖 | config/compliance/detectors.yaml |
| 违规类型 × 闸门 × 场景的动作和法规依据 | config/compliance/policy_matrix.yaml |

不要把动作写进检测器或 manifest；不要因为某个检测器“觉得严重”就直接在代码里拒答。

## 6. 三种异构接入方式

三种适配器最终都实现同一个行为：

~~~text
DetectionUnit + DetectContext + params
                  │
                  ▼
              检测实现
                  │
                  ▼
              DetectionHit[]
~~~

### 6.1 inprocess：轻量 Python

适合正则、词典、纯标准库算法和已经验证为轻量的模型。

~~~yaml
adapter: inprocess
entry: deerflow.compliance.detectors.my_detector.detector:MyDetector
~~~

特点：

- 由 Registry 反射加载类或对象；
- setup 只初始化一次；
- 每次 detect 返回值也会经过 validate_hit；
- 依赖直接进入主服务运行环境，不能放入 torch、OpenCV、spaCy 等重型依赖；
- 检测器异常由 Engine 捕获，但进程内的内存泄漏或全局状态污染仍可能影响主进程。

### 6.2 subprocess_cli：重依赖、非 Python 或需要进程隔离

适合以下情况：

- 需要独立虚拟环境的 torch、OpenCV、spaCy、Java/Go/Rust 二进制；
- 现有命令行工具不值得改造成 Python 包；
- 需要让崩溃、卡死和依赖冲突限制在子进程内。

manifest：

~~~yaml
adapter: subprocess_cli
command:
  - /opt/my-detector/venv/bin/python
  - /opt/my-detector/detect_cli.py
timeout_ms: 2000
cwd: /opt/my-detector
env:
  DETECTOR_MODEL: /opt/models/model.bin
~~~

协议：

- 每次调用向 stdin 写入一个 JSON 请求；
- 检测器从 stdout 输出 {"hits": [...]}，也接受裸数组 [...]；
- stdout 只能放协议 JSON；日志写 stderr；
- 超时、非零退出码、空/非法 JSON 都会被适配器转换为错误；
- 子进程返回的每个命中都会经过 JSON 反序列化和契约校验。

CLI 最小骨架：

~~~python
import json
import sys

from deerflow.compliance.adapters.serde import ctx_from_json, unit_from_json

request = json.load(sys.stdin)
unit = unit_from_json(request["unit"])
ctx = ctx_from_json(request["ctx"])
params = request.get("params", {})

hits = detect_with_external_runtime(unit, ctx, params)
print(json.dumps({"hits": hits}, ensure_ascii=False))
~~~

如果检测器完全由另一种语言实现，不需要导入 DeerFlow；只需实现同样的 JSON 结构，并用 JSON Schema 做自己的输入/输出校验。

### 6.3 http_service：已有服务、GPU 或独立扩缩容

适合已有模型服务、GPU 推理服务、需要独立发布/扩容/熔断的团队。

manifest：

~~~yaml
adapter: http_service
endpoint: http://detector-service:8080/detect
timeout_ms: 2000
headers:
  Authorization: Bearer detector-token
~~~

服务约定：

- 接收 POST /detect；
- body 与 CLI 请求完全相同：unit、ctx、可选 params；
- 返回 {"hits": [...]} 或 [...]；
- HTTP 4xx/5xx、超时、网络错误、非 JSON 响应均视为检测器失败；
- 服务端日志不得记录完整敏感 payload；应使用请求 ID、单位 ID、摘要或哈希；
- 服务端必须自行限制 body 大小、并发、模型输入长度和资源消耗。

当前 HTTP 适配器使用连接池客户端并延迟创建，因此 Registry import 不会访问网络。网络探活、模型预热和服务部署健康状态属于服务自身的运维职责。

### 6.4 跨语言 JSON 契约

生成或更新 Schema：

~~~bash
make compliance-export-schema
~~~

产物为 docs/compliance-detector-contract.schema.json。它描述：

- 请求：DetectionUnit、DetectContext、参数；
- 响应：DetectionHit[]；
- 文本、结构化字段和风险位置的形状；
- 合法的 gate、违规类型、severity 和风险位置类型。

跨语言接入的推荐流程：

1. 先生成 Schema；
2. 在服务或 CLI 侧生成对应 DTO；
3. 用固定 JSON fixture 做 round-trip；
4. 故意测试非法违规类型、越界置信度、非法 severity 和缺失字段；
5. 再接入真实模型。

注意：当前线协议是 JSON，DetectionUnit 不直接携带原始二进制。图像、视频、音频或超大文件检测应先做受控预处理，使用结构化字段、有限文本和受控 artifact 引用；不要把任意宿主路径或整个二进制塞进 raw，也不要让远程服务自行读取 Agent 宿主文件系统。

## 7. 异构检测器的设计建议

### 7.1 先划分适配边界，再选择技术

不要为了适配现有模型改动 Engine。推荐把变化限定在三层之一：

| 变化 | 推荐位置 |
|---|---|
| 业务对象如何转成文本/字段/媒体引用 | Normalizer 或专用预处理器 |
| 现有模型如何被调用 | inprocess 实现、CLI 包装器或 HTTP 服务 |
| 模型返回如何转成统一命中 | 适配器侧 serde/转换层 |

Engine、Router、Policy 和闸门不应知道模型名称、训练框架、供应商 API 或文件格式。

### 7.2 重型模型不要伪装成轻量检测器

如果一次检测需要加载 GPU 模型、网络调用或数百毫秒以上推理：

- 不要在主进程 import 时加载；
- 不要声明 cost_hint: light；
- 不要把 heavy 检测器挂到 ContextGate；
- 用 subprocess_cli 或 http_service；
- 明确 timeout_ms、模型启动成本、并发上限和失败策略；
- 用离线基线评估阈值，不要只看少量线上样本。

### 7.3 结构化输出优先于字符串拼接

如果检测器要判断字段组合，优先消费 field_items：

~~~text
metadata.camera_id
metadata.capture_time
metadata.latitude
metadata.longitude
~~~

只有真正面向文本的模型或规则才合并 text_items。这样能提供精确 field_path，避免命中后只能整段拒答。

### 7.4 多标签和不确定性

一个单元可以同时命中多个检测器、多个违规类型。检测器应：

- 每个独立违规类型生成独立 DetectionHit；
- 保留各自的 risk_locations；
- 不因自己判断“更像另一个类型”而覆盖其他类型；
- 对低置信度命中使用稳定的 severity 和 reason_code，把降级逻辑写清楚；
- 不把“发现敏感实体”直接等同于“高风险意图”。

## 8. 新检测器接入步骤

### 第一步：写能力设计

在写代码前明确：

- 所有者和唯一 detector_id；
- 负责的违规类型；
- 支持哪些闸门；
- 支持哪些 data_type；
- 输入是文本、字段还是媒体预处理结果；
- 需要什么风险位置；
- 延迟、内存、模型资产和网络依赖；
- 是否需要跨进程或独立服务。

特别确认：hardcoded_cred 由指南限定为 InputGate，re_identify 当前仅允许 OutputGate；不要为了方便在 manifest 中声明错误闸门。

### 第二步：创建目录和 manifest

推荐从以下模板复制：

~~~text
backend/packages/harness/deerflow/compliance/detectors/_template_detector/
~~~

复制后：

1. 改成非下划线目录名；
2. 将示例文件重命名为 detector.py 和 manifest.yaml；
3. 替换所有占位符；
4. 确保 detector_id、类属性和 manifest 完全一致。

### 第三步：实现检测器或外部服务

进程内实现只 import：

~~~python
deerflow.compliance.contract
~~~

不要 import Engine、Registry、Router、Policy、Agent state、其他检测器或评测数据。重依赖实现放在 CLI/HTTP 的独立运行环境中。

普通畸形输入应返回空命中；不能因为一条坏记录让整个引擎抛异常。初始化配置错误可以在 setup/注册阶段失败，让 CI 尽早发现。

### 第四步：加入中心开关

在 config/compliance/detectors.yaml 中先关闭：

~~~yaml
- id: my_detector
  enabled: false
~~~

确认严格注册和专项测试通过后再启用。参数覆盖只放这里，不要复制一份 manifest 能力声明。

### 第五步：编写测试

至少覆盖：

- 真实阳性；
- 真实阴性；
- 边界/近阴性；
- 空值、类型错误、超长输入和 malformed 输入；
- 风险位置准确性；
- Detector 协议；
- manifest 解析和 Registry 严格模式；
- data_type 和 gate 路由；
- CLI/HTTP JSON round-trip；
- 外部检测器超时、崩溃、非法输出；
- 多检测器/多违规类型共存；
- 策略矩阵动作；
- 审计字段和敏感内容截断；
- 输出闸门持久化内容不含原始风险文本；
- 流式 compliance_retract 事件。

### 第六步：离线基线和冻结回归

为检测器保留不可变的测试集，至少包含阳性、阴性和近阴性。冻结后：

- 记录数据集 SHA-256；
- 记录规则/模型版本；
- 单独运行原始检测器基线；
- 再运行完整 ComplianceEngine；
- 逐行比较命中类型、位置和动作；
- 报告 precision、recall、F1、accuracy；
- 不得为了通过冻结集修改冻结数据或添加样本 ID 特判。

### 第七步：性能和预发布验收

至少测量 30 次重复调用，分别覆盖：

- 单类型阳性；
- 多类型阳性；
- 阴性；
- 外部服务冷启动和热调用；
- P50、P95、最大耗时；
- 子进程/HTTP timeout；
- OutputGate retract 延迟。

最后执行严格 Registry、专项测试、全量合规测试和 preflight。

## 9. 推荐命令

从仓库根目录：

~~~bash
# 导出跨语言契约
make compliance-export-schema

# 严格构建 Registry
cd backend
PYTHONPATH=.:packages/harness uv run python -c '
from deerflow.compliance.registry import build_registry
r = build_registry(
    config_path="../config/compliance/detectors.yaml",
    strict=True,
)
print([(x.detector_id, x.adapter_name, x.enabled) for x in r.all()])
'

# 运行单个检测器专项测试
PYTHONPATH=.:packages/harness uv run pytest \
  tests/test_compliance_<detector>.py -v

# 运行适配器、Registry、契约和解耦测试
PYTHONPATH=.:packages/harness uv run pytest \
  tests/test_compliance_adapters.py \
  tests/test_compliance_contract.py \
  tests/test_compliance_registry.py \
  tests/test_compliance_decoupling.py -q

# 运行全部合规测试
PYTHONPATH=.:packages/harness uv run pytest tests/test_compliance_*.py -q

# Python 代码检查
PYTHONPATH=.:packages/harness uv run ruff check \
  packages/harness/deerflow/compliance \
  tests/test_compliance_<detector>.py
~~~

提交前还应运行项目既有的模型/闸门评测和前端合规测试，不要只跑检测器单测。

## 10. 故障排查

### 检测器没有被调用

依次检查：

1. 目录下是否确实叫 manifest.yaml；
2. detector_id 是否与中心配置一致；
3. enabled 是否为 true；
4. 当前闸门是否在 manifest 中声明；
5. 单元的 data_type 是否匹配；
6. type-specific 检测器是否因为 data_type=None 被路由排除；
7. Registry 是否在非严格模式下记录了 skip 日志。

### 命中了但没有动作

检查：

- violation_type 是否在策略矩阵中；
- 当前 gate 和解析出的 scene 是否是预期值；
- 是否落入 _unknown；
- 矩阵单元是否为 null；
- 是否只有 warn/manual_review 等不改变 payload 的动作；
- 命中是否在错误闸门被 Engine 丢弃。

### 脱敏后仍然暴露原文

通常是 risk_locations 不可用：

- char_span 不是 start:end；
- 起止位置对应的不是原文；
- 没有填写 text 且偏移失效；
- 结构化风险没有提供 field_path；
- 检测器只返回了“有风险”，没有返回“风险在哪里”。

如果无法精确定位，当前动作层会保守拒答，不能返回未修改原文。

### 外部服务误报合规通过

确认：

- 入口返回的是 JSON，而不是把日志写入 stdout；
- response 是 {"hits": [...]} 或裸数组；
- confidence 在 [0,1]；
- severity 和 risk_location.kind 使用合法枚举；
- 服务端与 DeerFlow 使用同一份 Schema；
- 服务超时是否被当成失败诊断，而不是空命中。

## 11. 不允许的做法

- 在检测器中直接返回 refuse、desensitize 等策略动作；
- 用手机号、坐标、样本 ID 等固定测试短语作为特判；
- 读取 is_positive、expected_action 或冻结集标签；
- 把 scene_hint 当成授权结论；
- 在主服务 import 时加载大型模型或访问网络；
- 让 ContextGate 调用 heavy 检测器；
- 把整篇原文放入 evidence 或审计日志；
- 只在前端 CSS 隐藏风险，后端仍把原文写入消息、checkpoint 或 transcript；
- 让一个检测器异常中断其他检测器；
- 修改 Engine、Router 或 Policy 以适配某个模型的私有输出格式；
- 为了绕过协议校验，把非法命中降级成空结果。

## 12. 最终验收清单

- [ ] detector_id、类属性、manifest、中心配置一致。
- [ ] manifest 的违规类型、闸门、数据类型和开销声明真实。
- [ ] heavy 检测器未挂到 ContextGate。
- [ ] 进程内实现只依赖公开契约；重依赖已隔离。
- [ ] setup 可重复、无 import-time 网络/大模型加载。
- [ ] 阳性、阴性、近阴性、畸形和超长输入都有测试。
- [ ] 每个命中都有可操作的风险位置或明确说明为何不可定位。
- [ ] 同时命中的多个违规类型不会互相覆盖。
- [ ] CLI/HTTP 返回通过 JSON Schema 和 validate_hit。
- [ ] Registry strict=True 通过。
- [ ] gate/data_type 路由测试通过。
- [ ] 场景由可信解析或 _unknown 决定，未使用客户端 hint 放权。
- [ ] 策略动作来自 Policy Matrix，检测器没有业务处置逻辑。
- [ ] 审计记录包含动作、依据、场景、命中和诊断，且没有整段敏感原文。
- [ ] OutputGate 的状态、checkpoint、transcript 和 SSE 都不保留原始风险内容。
- [ ] 冻结回归与完整 Engine 基线一致。
- [ ] P50/P95/最大耗时和失败行为已记录。
- [ ] 全量合规测试、preflight 和相关前端测试通过。

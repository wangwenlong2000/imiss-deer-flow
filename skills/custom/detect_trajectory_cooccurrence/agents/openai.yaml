name: detect_trajectory_cooccurrence
display_name: 多目标时空伴行检测
short_description: 用户两两在同 geohash 网格 + 时间窗重叠的伴行检测。
default_prompt: "Use $detect_trajectory_cooccurrence when the user asks to detect groups of users co-occurring in same time-space windows, identify suspicious gathering events, or perform contact tracing / trajectory companion mining. Run python3 scripts/detect_trajectory_cooccurrence.py and summarize summary.json."

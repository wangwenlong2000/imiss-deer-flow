# Skill: flood-risk-mapping
skill:
  name: flood-risk-mapping
  version: 0.1.0
  language:
    metadata: en
    report: zh-CN

io:
  outputs_dir: ./outputs
  subdir: flood-risk-mapping

logging:
  level: INFO

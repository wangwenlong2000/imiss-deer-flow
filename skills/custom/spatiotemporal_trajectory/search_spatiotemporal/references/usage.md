# search_spatiotemporal Usage

Preferred input is `cleaned_points.jsonl`, `grid_points.jsonl`, or trajectory evidence JSONL. This Skill is a local-file retrieval bridge; use `search_spatiotemporal` only when the user explicitly asks for the indexed trajectory backend.

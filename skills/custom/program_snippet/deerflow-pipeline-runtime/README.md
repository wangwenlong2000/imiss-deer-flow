# DeerFlow Pipeline Runtime

This directory hosts the shared `deerflow.pipeline` Python package used by
the public code-business-DAG skills.

Add this directory to `sys.path` before importing:

```python
from deerflow.pipeline import CloudLLMReasoner, PipelineGraphBuilder
```

The package files were moved here from:

```text
skills/public/deerflow-pipeline-runtime/deerflow/pipeline/
```
